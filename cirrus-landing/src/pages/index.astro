---
// `/` — the English home page. The page itself is components/pages/HomePage.astro,
// shared with the localized homes under pages/[lang]/. This file stays exactly
// where it is so the site root can never move.
import HomePage from '../components/pages/HomePage.astro';
---

<HomePage lang="en" />
