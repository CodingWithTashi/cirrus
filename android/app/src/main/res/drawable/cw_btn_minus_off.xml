<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing to take away. Visibly inert rather than absent, so the control
     does not move under the thumb the moment the count reaches zero. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/cw_surface_low"/>
    <corners android:radius="999dp"/>
    <stroke android:width="1dp" android:color="@color/cw_surface"/>
</shape>
