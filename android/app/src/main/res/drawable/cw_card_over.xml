<?xml version="1.0" encoding="utf-8"?>
<!-- Over today's line. Same card, Relapse-Red rim — matching the ring
     treatment on Home, and deliberately nothing louder: the copy that goes
     with it is "no shame". -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/cw_surface"
        android:endColor="@color/cw_surface_low"/>
    <corners android:radius="@dimen/cw_radius"/>
    <stroke android:width="1dp" android:color="@color/cw_danger_line"/>
</shape>
