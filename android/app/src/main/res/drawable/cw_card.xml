<?xml version="1.0" encoding="utf-8"?>
<!-- The Cirrus card. RemoteViews cannot render a BoxShadow, so the glow the
     app draws in Dart is out of reach; the vertical fall from surface to
     surface_low plus a 1dp border is what reads as "Cirrus" without it. A fake
     glow that looks wrong is worse than no glow. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/cw_surface"
        android:endColor="@color/cw_surface_low"/>
    <corners android:radius="@dimen/cw_radius"/>
    <stroke android:width="1dp" android:color="@color/cw_border"/>
</shape>
