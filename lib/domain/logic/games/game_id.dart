/// The panic games, in switcher order. `.name` is the wire value in the
/// journey codec, analytics and the `?g=` query — renaming one reclassifies
/// every stored best.
enum GameId { tiles, blocks, orbs }
